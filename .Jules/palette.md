## 2026-07-21 - Adding ARIA labels to Icon-Only Buttons
**Learning:** Next.js application has several icon-only buttons for actions like Edit, Add Progress, Add Spending, and Delete which require descriptive ARIA labels to maintain accessibility for screen reader users.
**Action:** Routinely check for newly added icon-only `<Button>` components and ensure that they contain the `aria-label` attribute.
